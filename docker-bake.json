{
  "variable": {
    "DOCKER_METADATA_OUTPUT_TAGS": {
      "default": "ghcr.io/snapp-incubator/jira-msteams-proxy:latest"
    }
  },
  "group": {
    "default": {
      "targets": ["jira-msteams-proxy"]
    }
  },
  "target": {
    "jira-msteams-proxy": {
      "dockerfile": "Dockerfile",
      "tags": ["${DOCKER_METADATA_OUTPUT_TAGS}"]
    }
  }
}
